add_library(Emu Emu.h Emu.cpp)
if(DEFINED TRACE)
    verilate(Emu SOURCES "${DUT_DIR}/TestTop.v" "${DUT_DIR}/plusarg_reader.v" TRACE)
else()
    verilate(Emu SOURCES "${DUT_DIR}/TestTop.v" "${DUT_DIR}/plusarg_reader.v")
endif()